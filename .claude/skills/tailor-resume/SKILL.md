---
name: tailor-resume
description: 根据用户提供的岗位 JD，生成一份高度适配该岗位的单页 A4 HTML 简历。当用户给出招聘 JD / 岗位描述 / 职位要求并想要"定制简历""针对这个岗位改简历""生成专属简历""简历适配""投某某公司的简历"时触发。即使用户没明确说"用 skill"，只要意图是把简历往某个具体岗位靠拢、突出适配性，就应该使用此技能。输出的简历风格（白底卡片 + 左侧黑色色条标题、A4 单页、纯文本标签无 icon/emoji）与项目里 cv/index.html、resume/index.html 保持一致。
---

# 定向简历生成技能

## 这个技能做什么

用户给出一个岗位 JD，你产出**一份针对该岗位定制的单页 A4 HTML 简历**，放在 `resume/` 目录下，文件名形如 `resume_<公司或岗位>.html`。核心价值是**适配性**：不是把所有经历平铺，而是按 JD 的职责与要求重新编排、取舍、排序、措辞，让 HR 一眼看出"这个人就是为这个岗位来的"。

## 信息来源（务必遵守）

用户是**王京**，他的全部背景以这两个文件为**唯一权威来源**，每次生成前都要读取（信息可能更新，不要凭记忆）：

- `cv/index.html` — 精简版简历，含个人信息、教育、科研、实习、项目、获奖、证书、技能、爱好
- `resume/index.html` — 详细版简历，项目描述更完整

生成前先 Read 这两个文件，把可用的"原材料"梳理出来。为加速 JD 适配，`references/profile-inventory.md` 里有一份**项目清单索引**（每个项目的名称 / 技术栈 / 担任角色 / 成果 / 链接），用它快速挑选该 JD 该强调哪些项目。若索引与 cv/resume 实际内容冲突，**以 cv/resume 为准**并提示用户更新索引。

## JD 适配方法论（这是技能的灵魂）

定向简历 ≠ 把通用简历换个标题。按下面的步骤做适配：

1. **拆解 JD**：把 JD 的"职位职责"和"条件要求"各拆成几条关键能力点（如"Web 跨平台开发""Vue""AI 提效工具""大模型落地""团队协作"）。

2. **能力点 ↔ 原材料映射**：对每个能力点，从项目清单里挑出**最对口**的 1-3 个项目/经历去支撑。没素材的点诚实弱化或省略，不要硬凑——硬凑反而暴露短板。

3. **重排顺序**：与 JD 最相关的板块/项目**前置**。例如 JD 是"跨平台 Web 开发"就把"跨平台/Web 项目经验"放前面；JD 是"AI 提效"就把"AI 提效与智能化工具开发"放前面。JD 强调的能力点对应的技能标签用 `primary` 样式高亮——`primary` 是**灰底 + 黑字加粗**（`color:#1a1a1a; font-weight:700`），与普通标签同底色，仅靠更黑更粗的字重区分，避免深色填充块过于刺眼。

4. **改写措辞**：用 JD 的**关键词原词**改写项目描述。JD 说"跨平台架构与组件体系"，项目描述里就用"架构设计 / 组件复用 / 多端一致性"这类词；JD 说"大模型能力落地开发流程"，就明确点出"将 LLM/Agent 能力落地到 XX 流程"。这是让 ATS（简历筛选系统）和 HR 命中关键词的关键。

5. **新增"个人优势"段**：放在最顶部、教育背景之前。用 1 段（最多 2 段）把"我为什么适配这个岗位"讲清楚，直接呼应 JD 的核心要求，并放量化成果（Star 数、用户数、GPA 排名、获奖）。

6. **取舍**：单页 A4 容量有限。与 JD 无关的板块（如投前端岗时的"体育奖项""健身博主"）果断删掉，把空间让给相关内容。保留获奖/科研里**与岗位相关或能体现学习能力**的部分即可。

## 输出规范

- **单个 HTML 文件**，无外部依赖（不引 FontAwesome 等 CDN；图标/emoji 一律不用）。头像走站内相对路径 `/imgs/life_and_others/myavtar.png`。
- **A4 单页**：用 `.page` 容器（`width: 210mm; min-height: 297mm`），含 `@media print` 打印适配，Ctrl+P 可另存为干净 A4 PDF。完整 CSS 见 `assets/template.html`——**直接复用该文件的 CSS**，不要重新发明样式。
- **风格对齐 cv/index.html**：居中 header（头像 + 信息块）、左侧 4px 黑色色条 `.section-title`、`.subsection` 间浅灰分隔线、`.label-link`（灰）/ `.github-label-link`（绿）两类标签链接。
- **教育背景必须包含学院与研究方向**：每条教育经历的 `.subsection-title` 必须写"学校 · 学院 · 专业 · 学历"（学院不可省，如"生命科学与技术学院""信息工程学院"，从 cv/resume 或 profile-inventory 取真实学院名）。`.date` 行之后追加一行 `<div class="tech-line"><em>研究方向：</em>方向1 · 方向2 · 方向3</div>`。方向关键词从 JD 职责中提取（如 JD 提"信号处理""医学影像""智能化应用"，就把对应方向写进去），让教育背景直接呼应岗位需求。硕士研究方向偏科研/学术，本科研究方向偏基础/技术。
- **专业技能用流式文本，不用按钮/标签样式**：去掉 `.skill` `.skill.primary` 的灰底边框按钮感，改用 `.skill-line` + `.skill-cat` + `.skill-hl` 的纯文本流式排版。格式为两行：`<div class="skill-line"><span class="skill-cat">分类名：</span><span class="skill-hl">核心技能1</span> · 技能2 · <span class="skill-hl">核心技能3</span> · ...</div>`。`.skill-cat` 分类标题加粗黑色，`.skill-hl` 核心技能加粗深色突出，其余技能保持灰色常规字重，用 ` · ` 中点分隔。紧凑、省空间、更像传统专业简历。**该板块可按需删除**：当单页空间紧张、或 JD 不强调技能清单、或用户要求精简时，可整块去掉专业技能板块，把空间让给项目/成果——技能关键词已在"个人优势"与项目描述中体现即可。
- **红字突出重点内容**（`.hl-red`，`color:#fd0000; font-weight:600`）：把最抓眼的量化成果与硬资质标红，让 HR 扫读时第一时间命中。必标红的对象包括：GitHub Star 数（如 `200+ Star`、`70+ Star`）、用户规模（`150+ 用户社群`）、核心荣誉资质（`四川省优秀毕业生 · 推免生`、`发明专利 1 项，软件著作权 5 项`）等。标红要克制——每块 1-3 处即可，全红等于不红。`.hl-red` 样式定义需随 CSS 一起写入生成的 html（template 里没有，要自己加）。
- **获奖/奖学金重复次数用 ×N 标注**：同一奖项多次获得时写"国家励志奖学金 ×2""电子科技大学优秀研究生 ×2""学业奖学金一等奖 ×2"，不要拆成多行也不要省略次数——×N 本身就是量化亮点。
- 为塞进单页 A4，正文 12px、标题 14px，间距收紧；内容超出/不足一页时调整间距或增删板块，但**优先保证单页**。
- header 的个人信息行**不要写政治面貌**（如"中共党员"）。格式参考：`男 · 24岁 · 硕士`，下接电话/邮箱/微信/GitHub/主页。
- header 顶部加一行"求职意向：XX 岗位 / XX 方向（公司名）"，用蓝色 `.target` 样式。**通用版（一份多投）**时去掉公司名与地域，只写"求职意向：XX 岗位 / XX 方向"。
- 保留项目里的真实链接（GitHub、论文、用户群、证书页），href 直接复用 cv/resume 里的原 URL。
- **每个项目都要写「项目职责」+「项目成果」两条目**：项目经验区每个 `.subsection` 的 `<ul>` 末尾加 `<li><strong>项目职责：</strong>...</li>`（"角色 + 职责范围"句式，如实反映独立负责人 / 主导 / 主要负责人）和 `<li><strong>项目成果：</strong>...</li>`（量化产出：Star 数、用户数、论文、获奖、上线规模等，能标红的用 `.hl-red`）。两者都是硬性要求，不可省略——投递系统会校验"职责及成果"，缺一会被判不完整。

## 工作流程

1. Read `cv/index.html` 和 `resume/index.html`，Read `references/profile-inventory.md`。
2. 与用户确认 JD（如果用户已在消息里给出就直接用；若 JD 模糊，问一句要"公司+岗位名"用于命名文件和求职意向行）。
3. 按"JD 适配方法论"拆解、映射、重排、改写。
4. Read `assets/template.html`，基于它的 CSS 结构填充定制内容，Write 到 `resume/resume_<公司或岗位>.html`。
5. **更新简历墙数据源**：把新简历追加进 `resume/tailored.json`（这是卡片墙 `resume/tailored.html` 的数据源，页面会读取它动态渲染卡片，所以这一步**必做**，否则新简历不会出现在墙上）。追加规范见下方"简历墙数据源规范"。
6. 生成后简述：你按 JD 强调了哪几点、前置了哪些板块、删掉了哪些无关内容，让用户快速核对适配方向是否正确。并提示可用浏览器 Ctrl+P 预览 A4。

## 简历墙数据源规范

`resume/tailored.json` 是一个 JSON 数组，每个元素对应一份定制简历的一张卡片。生成新简历后，向数组**末尾追加**一个对象。字段：

```json
{
  "index": "07",
  "company": "公司 · 批次/地点",
  "role": "岗位名称",
  "direction": "方向短语（如 AI Agent / RAG / 算法）",
  "tags": ["标签1", "标签2", "标签3", "标签4"],
  "date": "YYYY.MM",
  "url": "/resume/resume_xxx.html",
  "match": "high" 
}
```

要点：
- `index`：两位序号，接续现有最大值 +1（现有最大是 06，新简历就是 07）。同时把 `resume/tailored.html` 里 section 标题硬编码的 `/ 06` 改成实际总数（例如 7 份就改成 `/ 07`）。
- `url`：站内相对路径，指向第 4 步生成的 html。
- `match`：匹配度。填 `"high"` 显示"高匹配"绿徽章；普通岗位留空字符串 `""`。判断依据：JD 核心要求与你背景高度对口（如 AI Agent 岗 + 你有 NEURA）才算 high，不要滥用。
- `tags`：3–4 个，用 JD 关键词，与简历里 primary 高亮的技能呼应。
- 追加时**保持 JSON 合法**（逗号、引号、无尾逗号）。追加后无需改 `tailored.html` 卡片结构——它会 fetch 这个 JSON 渲染。
- 若是**重新生成/更新**一份已有简历（同公司同岗），不要新增卡片，而是更新 JSON 里对应那条记录（按 url 或 company 匹配）。


## 何时问、何时直接做

- JD 清晰、公司/岗位名明确 → 直接生成，不要反复确认。
- JD 只给职责没给公司名 → 生成前问一句公司名（用于文件名和求职意向行），其余直接做。
- 用户后续要求调整（换风格、加内容、改页数）→ 直接改，不必重走全流程。
